name: Parser — каждые 30 минут

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  parse:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}

      - name: Установка зависимостей
        run: pip install -r requirements.txt

      - name: Запуск парсера
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          MINI_APP_URL: ${{ secrets.MINI_APP_URL }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          PAYMENT_PROVIDER_TOKEN: ${{ secrets.PAYMENT_PROVIDER_TOKEN }}
        run: |
          cd bot
          python main.py parse

      - name: Сохранение данных
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          git diff --staged --quiet || git commit -m "chore: update parser data [skip ci]"
          git push
